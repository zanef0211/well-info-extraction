# 数据库初始化指南

## 数据库配置

### 连接信息

- **数据库类型**: PostgreSQL
- **数据库名**: `wellinfo`
- **用户名**: `wellinfo`
- **密码**: `wellinfo2026`
- **管理员账号**: `postgres` / `1437@2026`
- **主机**: `localhost`
- **端口**: `5432`

### 连接URL

```
postgresql://wellinfo:wellinfo2026@localhost:5432/wellinfo
```

## 初始化步骤

### 步骤1: 安装依赖

```bash
pip install -r requirements.txt
```

确保安装了 `psycopg2-binary`:
```bash
pip show psycopg2-binary
```

### 步骤2: 确保 PostgreSQL 服务运行

#### Windows

```bash
# 检查 PostgreSQL 服务状态
sc query postgresql-x64-15

# 如果未运行,启动服务
net start postgresql-x64-15
```

#### Linux/Mac

```bash
# 检查 PostgreSQL 状态
sudo systemctl status postgresql

# 如果未运行,启动服务
sudo systemctl start postgresql
```

### 步骤3: 运行初始化脚本

推荐使用简化脚本:

```bash
python scripts/init_database_simple.py
```

脚本会执行以下操作:

1. ✅ 连接到 PostgreSQL (管理员账号: postgres/1437@2026)
2. ✅ 创建数据库 `wellinfo`
3. ✅ 创建用户 `wellinfo` / `wellinfo2026`
4. ✅ 授予权限
5. ✅ 创建表结构 (6个表)
6. ✅ 创建触发器
7. ✅ 测试连接

### 步骤4: 验证数据库

连接到数据库验证:

```bash
# 使用 psql 连接
psql -U wellinfo -d wellinfo -h localhost
# 输入密码: wellinfo2026

# 查看表
\dt

# 退出
\q
```

## 表结构

数据库包含以下6个表:

| 表名 | 说明 |
|------|------|
| `wells` | 井信息表 - 存储油气井基本信息 |
| `documents` | 文档表 - 存储上传文档信息 |
| `extracted_data` | 提取数据表 - 存储提取的结构化数据 |
| `quality_reports` | 质量评估表 - 存储质量评估结果 |
| `processing_logs` | 处理日志表 - 存储处理日志 |
| `users` | 用户表 - 存储系统用户 |

详细的表结构说明请查看: `scripts/README.md`

## 手动创建数据库(可选)

如果脚本无法运行,可以手动创建:

### 1. 使用 psql 命令

```bash
# 连接到 PostgreSQL (管理员)
psql -U postgres -d postgres -h localhost
# 输入密码: 1437@2026

# 创建数据库
CREATE DATABASE wellinfo;

# 创建用户
CREATE USER wellinfo WITH PASSWORD 'wellinfo2026';

# 授予权限
GRANT ALL PRIVILEGES ON DATABASE wellinfo TO wellinfo;
GRANT ALL ON SCHEMA public TO wellinfo;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON TABLES TO wellinfo;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON SEQUENCES TO wellinfo;

# 退出
\q
```

### 2. 连接到新数据库并创建表

```bash
# 连接到 wellinfo 数据库
psql -U wellinfo -d wellinfo -h localhost
# 输入密码: wellinfo2026

# 执行表创建SQL
\i scripts/create_tables.sql

# 或者手动执行(见下文的SQL)
```

## 创建表的SQL语句

如果需要手动创建表,可以使用以下SQL:

```sql
-- 1. 井信息表
CREATE TABLE IF NOT EXISTS wells (
    id SERIAL PRIMARY KEY,
    well_no VARCHAR(50) UNIQUE NOT NULL,
    well_name VARCHAR(200),
    oilfield VARCHAR(100),
    block VARCHAR(100),
    well_type VARCHAR(50),
    well_pattern VARCHAR(50),
    well_class VARCHAR(50),
    latitude DECIMAL(10, 6),
    longitude DECIMAL(10, 6),
    elevation DECIMAL(10, 2),
    ground_elevation DECIMAL(10, 2),
    drill_date DATE,
    completion_date DATE,
    status VARCHAR(20) DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 2. 文档表
CREATE TABLE IF NOT EXISTS documents (
    id SERIAL PRIMARY KEY,
    well_no VARCHAR(50) NOT NULL,
    document_id VARCHAR(100) UNIQUE,
    filename VARCHAR(255) NOT NULL,
    file_path VARCHAR(500),
    file_size BIGINT,
    file_extension VARCHAR(10),
    mime_type VARCHAR(100),
    document_type VARCHAR(50),
    category VARCHAR(50),
    upload_date DATE,
    uploaded_by VARCHAR(100),
    status VARCHAR(20) DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (well_no) REFERENCES wells(well_no) ON DELETE CASCADE
);

-- 3. 提取数据表
CREATE TABLE IF NOT EXISTS extracted_data (
    id SERIAL PRIMARY KEY,
    document_id VARCHAR(100) NOT NULL,
    well_no VARCHAR(50) NOT NULL,
    field_name VARCHAR(100) NOT NULL,
    field_value TEXT,
    field_type VARCHAR(20),
    confidence DECIMAL(5, 2),
    is_valid BOOLEAN DEFAULT TRUE,
    validation_errors JSONB,
    source VARCHAR(20),
    extracted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (document_id) REFERENCES documents(document_id) ON DELETE CASCADE,
    FOREIGN KEY (well_no) REFERENCES wells(well_no) ON DELETE CASCADE,
    UNIQUE(document_id, field_name)
);

-- 4. 质量评估表
CREATE TABLE IF NOT EXISTS quality_reports (
    id SERIAL PRIMARY KEY,
    document_id VARCHAR(100) UNIQUE NOT NULL,
    well_no VARCHAR(50) NOT NULL,
    completeness DECIMAL(5, 2),
    accuracy DECIMAL(5, 2),
    consistency DECIMAL(5, 2),
    confidence DECIMAL(5, 2),
    overall_score DECIMAL(5, 2),
    quality_level VARCHAR(20),
    issues JSONB,
    suggestions JSONB,
    validated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (document_id) REFERENCES documents(document_id) ON DELETE CASCADE,
    FOREIGN KEY (well_no) REFERENCES wells(well_no) ON DELETE CASCADE
);

-- 5. 处理日志表
CREATE TABLE IF NOT EXISTS processing_logs (
    id SERIAL PRIMARY KEY,
    document_id VARCHAR(100),
    well_no VARCHAR(50),
    stage VARCHAR(50),
    status VARCHAR(20),
    duration_ms INTEGER,
    message TEXT,
    error_message TEXT,
    metadata JSONB,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (document_id) REFERENCES documents(document_id) ON DELETE SET NULL
);

-- 6. 用户表
CREATE TABLE IF NOT EXISTS users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE,
    hashed_password VARCHAR(255) NOT NULL,
    full_name VARCHAR(100),
    role VARCHAR(20) DEFAULT 'user',
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 创建自动更新 updated_at 的触发器
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = CURRENT_TIMESTAMP;
    RETURN NEW;
END;
$$ language 'plpgsql';

-- 为每个表添加触发器
CREATE TRIGGER update_wells_updated_at
BEFORE UPDATE ON wells FOR EACH ROW
EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_documents_updated_at
BEFORE UPDATE ON documents FOR EACH ROW
EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_extracted_data_updated_at
BEFORE UPDATE ON extracted_data FOR EACH ROW
EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_quality_reports_updated_at
BEFORE UPDATE ON quality_reports FOR EACH ROW
EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_users_updated_at
BEFORE UPDATE ON users FOR EACH ROW
EXECUTE FUNCTION update_updated_at_column();
```

## 常见问题

### Q1: 提示 "password authentication failed for user 'postgres'"

**原因**: PostgreSQL 管理员密码不正确

**解决**:
1. 检查 PostgreSQL 安装时的管理员密码
2. 修改脚本中的 `ADMIN_DB_URL` 为正确的密码
3. 确认密码是 `1437@2026`

### Q2: 提示 "connection refused"

**原因**: PostgreSQL 服务未运行或端口错误

**解决**:
1. 检查 PostgreSQL 服务是否运行
2. 确认端口是 5432
3. 检查防火墙设置

### Q3: 提示 "database 'wellinfo' already exists"

**原因**: 数据库已存在

**解决**:
- 脚本会询问是否删除并重新创建
- 输入 `yes` 重新创建
- 输入 `no` 保留现有数据库

### Q4: 提示 "role 'wellinfo' already exists"

**原因**: 用户已存在

**解决**:
- 脚本会询问是否修改密码
- 输入 `yes` 修改密码
- 输入 `no` 保留现有用户

## 测试连接

### 使用 Python 测试

```python
import psycopg2

try:
    # 连接数据库
    conn = psycopg2.connect(
        host="localhost",
        port="5432",
        database="wellinfo",
        user="wellinfo",
        password="wellinfo2026"
    )

    cursor = conn.cursor()

    # 测试查询
    cursor.execute("SELECT version();")
    version = cursor.fetchone()[0]
    print(f"✅ PostgreSQL 版本: {version.split(',')[0]}")

    # 查看表
    cursor.execute("""
        SELECT table_name
        FROM information_schema.tables
        WHERE table_schema = 'public'
        ORDER BY table_name;
    """)
    tables = cursor.fetchall()
    print(f"✅ 已创建 {len(tables)} 个表:")
    for table in tables:
        print(f"    - {table[0]}")

    cursor.close()
    conn.close()

    print("\n✅ 数据库连接测试成功!")

except Exception as e:
    print(f"❌ 连接失败: {e}")
```

### 使用应用配置测试

```python
from config.settings import get_settings

settings = get_settings()
print(f"数据库 URL: {settings.DATABASE_URL}")
```

## 下一步

数据库初始化完成后:

1. ✅ 测试数据库连接
2. ✅ 启动应用服务
3. ✅ 上传文档测试井号识别
4. ✅ 查看提取结果

```bash
# 启动服务
python main.py

# 访问 API 文档
# http://localhost:8000/docs

# 测试井号优先的流程
python test_wellno_first.py
```

## 相关文档

- [数据库初始化详细说明](../scripts/README.md)
- [核心处理流程覆盖分析](核心处理流程覆盖分析.md)
- [井号优先流程](../test_wellno_first.py)

## 支持

如果遇到问题:

1. 检查 PostgreSQL 服务状态
2. 查看错误日志
3. 确认用户名和密码正确
4. 查看相关文档
